import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_admin/financials/provider-config')({
  component: () => <div>Provider Config (Not Implemented)</div>,
})
